Proper documentation will be written at some point. For now, the follwing bash commands may help you get up and running.

cd static 
curl "http://www.srcf.ucam.org/cucats/style.css" | sed s.fonts/./static/fonts/. > style.css
cd ..

python manage.py schemamigration --initial main
python manage.py schemamigration --initial questionnaire
python manage.py syncdb
python manage.py migrate
python manage.py runserver
